import { MsCompHostDirective } from './ms-comp-host.directive';

describe('MsCompHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MsCompHostDirective();
    expect(directive).toBeTruthy();
  });
});
