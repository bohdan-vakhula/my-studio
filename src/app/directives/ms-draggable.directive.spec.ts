import { MsDraggableDirective } from './ms-draggable.directive';

describe('MsDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new MsDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
